// 8. Lê 2 valores e os escreve em ordem crescente
function ordemCrescente(a, b) {
    if (a < b) {
        console.log(`Ordem crescente: ${a}, ${b}`);
    } else {
        console.log(`Ordem crescente: ${b}, ${a}`);
    }
}